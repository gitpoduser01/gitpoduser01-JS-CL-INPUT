tasks:
  - init: sudo apt update && wget https://storage.googleapis.com/chrome-for-testing-public/123.0.6278.0/linux64/chrome-linux64.zip && unzip chrome-linux64.zip
  - command: sleep 15;sudo apt update && wget https://storage.googleapis.com/chrome-for-testing-public/123.0.6278.0/linux64/chromedriver-linux64.zip && unzip chromedriver-linux64.zip  && mkdir driver && mv chromedriver-linux64/chromedriver driver driver && rm chromedriver-linux64.zip
  - command: for i in {1..6}; do chmod 600 ~/.ssh/id_rsa && chmod 644 ~/.ssh/id_rsa.pub && ssh-keyscan github.com >> ~/.ssh/known_hosts; sleep 20; done
  - command: git remote set-url origin git@github.com:gitpoduser01/gitpoduser01-JS-CL-INPUT.git && sed "s/\r$//" commit-command.sh | bash
